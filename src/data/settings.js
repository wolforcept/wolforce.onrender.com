// Windows 98 Desktop - Settings Configuration

// Grid settings for desktop icons
export const gridSettings = {
  cellWidth: 75,
  cellHeight: 85,
  padding: 10,
};

// Default window settings
export const defaultWindowSettings = {
  width: 640,
  height: 480,
  minWidth: 200,
  minHeight: 150,
};

// UI scale settings
// Controls the size of taskbar, start menu, and related elements
// 1.0 = default size, 1.25 = 25% larger, 1.5 = 50% larger, etc.
export const uiSettings = {
  scale: 1.25,
};

