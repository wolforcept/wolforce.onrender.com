```js
const MAP_SVG_W = 1000;   // SVG viewBox width (don't change unless SVG differs)
const MAP_SVG_H = 507;    // SVG viewBox height
const MAP_SCALE = 7;    // Scale factor for game-grid → SVG coords
const MAP_TRANSLATE = { x: -3800, y: -1100 }; // Pixel offset applied after scale
```
